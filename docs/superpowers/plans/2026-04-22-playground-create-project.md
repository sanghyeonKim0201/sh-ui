# Playground → Create Project 구현 플랜

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 플레이그라운드에서 편집한 토큰을 base64로 실어 `@sh-ui/create --theme` 한 줄 명령어로 새 프로젝트에 반영한다.

**Architecture:** 세 단계로 나뉜 변경 — (1) 템플릿의 `tokens.css` / `sh_ui_tokens.dart` 에 섹션 마커 주석 삽입, (2) `@sh-ui/create` CLI 에 비대화형 플래그·theme 디코드·주입 로직 추가, (3) `apps/docs` 플레이그라운드 에 FAB + Dialog 로 명령어 빌더 UI 추가. 단계는 독립 PR 이며 반드시 1 → 2 → 3 순서 (앞 단계가 뒤 단계의 테스트 전제).

**Tech Stack:** Node.js (CLI) + ES modules, vitest, Next.js App Router + React 19, Base UI components (이미 설치됨).

**설계 스펙:** `docs/superpowers/specs/2026-04-22-playground-create-project-design.md`

**마커 규약 (스펙 refinement):** 단일 쌍이 아니라 섹션 구분 — `theme-colors-start/end` 와 `theme-radius-start/end`. 이유: dart 파일의 `ShUiColorTokens` 와 `ShUiRadiusTokens` 가 별도 클래스라 단일 쌍으로 감쌀 수 없음. CSS 도 동일 규약으로 통일해 `inject.js` API 를 단순화.

---

## Phase 1 — 템플릿 마커 삽입 (PR #1)

### Task 1: `nextjs-standalone` tokens.css 에 마커 삽입

**Files:**
- Modify: `packages/create/templates/nextjs-standalone/src/shared/styles/tokens.css`

기존 `:root { 색상 + radius + spacing + ... }` 단일 블록을 두 블록으로 분리. 색상 블록 주변을 `theme-colors` 마커로, `--radius` 선언 줄 주변을 `theme-radius` 마커로 감싼다.

- [ ] **Step 1: 파일을 아래 내용으로 교체**

```css
/* Generated by @sh-ui/tokens — do not edit directly */
/* base=neutral radius=md mode=light-dark */

/* sh-ui:theme-colors-start */
:root {
  --background: #FFFFFF;
  --background-subtle: #FAFAFA;
  --background-muted: #F5F5F5;
  --background-inverse: #0A0A0A;
  --foreground: #0A0A0A;
  --foreground-muted: #525252;
  --foreground-subtle: #A3A3A3;
  --foreground-inverse: #FFFFFF;
  --border: #E5E5E5;
  --border-strong: #D4D4D4;
  --primary: #171717;
  --primary-foreground: #FAFAFA;
  --primary-hover: #262626;
  --danger: #DC2626;
  --danger-foreground: #FFFFFF;
}
.dark {
  --background: #0A0A0A;
  --background-subtle: #171717;
  --background-muted: #262626;
  --background-inverse: #FFFFFF;
  --foreground: #FAFAFA;
  --foreground-muted: #A3A3A3;
  --foreground-subtle: #737373;
  --foreground-inverse: #0A0A0A;
  --border: #262626;
  --border-strong: #404040;
  --primary: #FAFAFA;
  --primary-foreground: #171717;
  --primary-hover: #E5E5E5;
  --danger: #DC2626;
  --danger-foreground: #FFFFFF;
}
/* sh-ui:theme-colors-end */

:root {
  /* sh-ui:theme-radius-start */
  --radius: 0.5rem;
  /* sh-ui:theme-radius-end */
  --space-0: 0px;
  --space-1: 4px;
  --space-2: 8px;
  --space-3: 12px;
  --space-4: 16px;
  --space-5: 20px;
  --space-6: 24px;
  --space-8: 32px;
  --space-10: 40px;
  --space-12: 48px;
  --space-16: 64px;
  --text-xs: 12px;
  --text-sm: 14px;
  --text-base: 16px;
  --text-lg: 18px;
  --text-xl: 20px;
  --text-2xl: 24px;
  --text-3xl: 30px;
  --text-4xl: 36px;
  --weight-regular: 400;
  --weight-medium: 500;
  --weight-semibold: 600;
  --weight-bold: 700;
  --shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.08);
  --shadow-md: 0 4px 12px rgba(0, 0, 0, 0.12);
  --shadow-lg: 0 8px 24px rgba(0, 0, 0, 0.15);
  --shadow-xl: 0 16px 48px rgba(0, 0, 0, 0.18);
  --duration-fast: 120ms;
  --duration-base: 160ms;
  --duration-slow: 200ms;
  --ease-standard: cubic-bezier(0.4, 0, 0.2, 1);
  --ease-emphasized: cubic-bezier(0.2, 0, 0, 1);
  --control-sm: 32px;
  --control-md: 40px;
  --control-lg: 48px;
  --border-width: 1px;
  --border-width-strong: 2px;
  --opacity-disabled: 0.5;
  --z-base: 0;
  --z-sticky: 100;
  --z-dropdown: 200;
  --z-overlay: 300;
  --z-modal: 400;
  --z-popover: 500;
  --z-toast: 600;
  --z-tooltip: 700;
  --bp-sm: 640px;
  --bp-md: 768px;
  --bp-lg: 1024px;
  --bp-xl: 1280px;
}
```

- [ ] **Step 2: 기존 smoke 테스트 재실행으로 회귀 없음 확인**

```bash
cd packages/create && pnpm test
```
기대: 모든 테스트 그린.

- [ ] **Step 3: 커밋**

```bash
git add packages/create/templates/nextjs-standalone/src/shared/styles/tokens.css
git commit -m "chore(create): nextjs-standalone 토큰에 theme 섹션 마커 삽입"
```

---

### Task 2: `ui-app-template` tokens.css 에 마커 삽입

**Files:**
- Modify: `packages/create/templates/ui-app-template/src/styles/tokens.css`

`ui-app-template` 의 tokens.css 는 `nextjs-standalone` 과 내용 동일. Task 1 과 같은 변경.

- [ ] **Step 1: 파일을 Task 1 Step 1 과 동일한 내용으로 교체**

(Task 1 의 CSS 블록을 그대로 복사. 파일 경로만 다름.)

- [ ] **Step 2: 기존 smoke 테스트 재실행**

```bash
cd packages/create && pnpm test
```
기대: 그린.

- [ ] **Step 3: 커밋**

```bash
git add packages/create/templates/ui-app-template/src/styles/tokens.css
git commit -m "chore(create): ui-app-template 토큰에 theme 섹션 마커 삽입"
```

---

### Task 3: `flutter-standalone` sh_ui_tokens.dart 에 마커 삽입

**Files:**
- Modify: `packages/create/templates/flutter-standalone/lib/sh_ui/foundation/sh_ui_tokens.dart:42-90`

`ShUiColorTokens.light` / `.dark` 정적 상수 둘을 `theme-colors` 마커 쌍으로 감싸고, `ShUiRadiusTokens` 의 `defaultRadius` 값을 `theme-radius` 마커 쌍으로 감싼다.

- [ ] **Step 1: `ShUiColorTokens` 내부의 `static const light` 직전에 marker-start, `static const dark` 선언 끝 `);` 직후에 marker-end 삽입**

파일의 42~76행 영역을 다음처럼 변경:

변경 **전**:
```dart
  static const light = ShUiColorTokens(
    background: Color(0xFFFFFFFF),
    ...
    dangerForeground: Color(0xFFFFFFFF),
  );

  static const dark = ShUiColorTokens(
    background: Color(0xFF0A0A0A),
    ...
    dangerForeground: Color(0xFFFFFFFF),
  );
}
```

변경 **후**:
```dart
  // sh-ui:theme-colors-start
  static const light = ShUiColorTokens(
    background: Color(0xFFFFFFFF),
    backgroundSubtle: Color(0xFFFAFAFA),
    backgroundMuted: Color(0xFFF5F5F5),
    backgroundInverse: Color(0xFF0A0A0A),
    foreground: Color(0xFF0A0A0A),
    foregroundMuted: Color(0xFF525252),
    foregroundSubtle: Color(0xFFA3A3A3),
    foregroundInverse: Color(0xFFFFFFFF),
    border: Color(0xFFE5E5E5),
    borderStrong: Color(0xFFD4D4D4),
    primary: Color(0xFF171717),
    primaryForeground: Color(0xFFFAFAFA),
    primaryHover: Color(0xFF262626),
    danger: Color(0xFFDC2626),
    dangerForeground: Color(0xFFFFFFFF),
  );

  static const dark = ShUiColorTokens(
    background: Color(0xFF0A0A0A),
    backgroundSubtle: Color(0xFF171717),
    backgroundMuted: Color(0xFF262626),
    backgroundInverse: Color(0xFFFFFFFF),
    foreground: Color(0xFFFAFAFA),
    foregroundMuted: Color(0xFFA3A3A3),
    foregroundSubtle: Color(0xFF737373),
    foregroundInverse: Color(0xFF0A0A0A),
    border: Color(0xFF262626),
    borderStrong: Color(0xFF404040),
    primary: Color(0xFFFAFAFA),
    primaryForeground: Color(0xFF171717),
    primaryHover: Color(0xFFE5E5E5),
    danger: Color(0xFFDC2626),
    dangerForeground: Color(0xFFFFFFFF),
  );
  // sh-ui:theme-colors-end
}
```

- [ ] **Step 2: `ShUiRadiusTokens.tokens` 정적 상수의 `defaultRadius` 라인을 marker 쌍으로 감쌈**

87~89행 영역을 다음처럼 변경:

변경 **전**:
```dart
  static const tokens = ShUiRadiusTokens(
    defaultRadius: 8.0,
  );
```

변경 **후**:
```dart
  static const tokens = ShUiRadiusTokens(
    // sh-ui:theme-radius-start
    defaultRadius: 8.0,
    // sh-ui:theme-radius-end
  );
```

- [ ] **Step 3: smoke 테스트 재실행으로 Flutter 시나리오 회귀 없음 확인**

```bash
cd packages/create && pnpm test
```
기대: `scenario 5 — flutter standalone` 포함 전체 그린.

- [ ] **Step 4: 커밋**

```bash
git add packages/create/templates/flutter-standalone/lib/sh_ui/foundation/sh_ui_tokens.dart
git commit -m "chore(create): flutter-standalone 토큰에 theme 섹션 마커 삽입"
```

---

### Task 4: 템플릿 마커 존재 검증 테스트

**Files:**
- Create: `packages/create/test/markers.test.js`

모든 템플릿 tokens 파일이 `theme-colors` 와 `theme-radius` 마커 쌍을 갖는지 assert. 미래에 새 템플릿을 추가하면 CI 가 자동으로 확인.

- [ ] **Step 1: 먼저 실패하는 테스트 작성 (마커 검증 함수 아직 없음)**

```javascript
import { describe, it, expect } from 'vitest';
import fs from 'fs-extra';
import path from 'node:path';
import { fileURLToPath } from 'node:url';

const __dirname = path.dirname(fileURLToPath(import.meta.url));
const TEMPLATES_DIR = path.join(__dirname, '..', 'templates');

/**
 * 각 템플릿에서 tokens 파일과 해당 파일이 가져야 하는 마커 섹션을 나열.
 * 새 템플릿이 생기면 여기에 엔트리 추가.
 */
const TOKEN_FILES = [
  {
    path: 'nextjs-standalone/src/shared/styles/tokens.css',
    commentPair: ['/*', '*/'],
  },
  {
    path: 'ui-app-template/src/styles/tokens.css',
    commentPair: ['/*', '*/'],
  },
  {
    path: 'flutter-standalone/lib/sh_ui/foundation/sh_ui_tokens.dart',
    commentPair: ['//', ''],
  },
];

const REQUIRED_SECTIONS = ['theme-colors', 'theme-radius'];

const makeMarker = (commentPair, label) => {
  const [open, close] = commentPair;
  return close
    ? `${open} sh-ui:${label} ${close}`
    : `${open} sh-ui:${label}`;
};

describe('템플릿 theme 마커', () => {
  for (const file of TOKEN_FILES) {
    for (const section of REQUIRED_SECTIONS) {
      const startMarker = makeMarker(file.commentPair, `${section}-start`);
      const endMarker = makeMarker(file.commentPair, `${section}-end`);

      it(`${file.path} 에 ${section} 마커 쌍이 있어야 함`, async () => {
        const abs = path.join(TEMPLATES_DIR, file.path);
        const content = await fs.readFile(abs, 'utf-8');
        expect(content).toContain(startMarker);
        expect(content).toContain(endMarker);
        expect(content.indexOf(startMarker)).toBeLessThan(content.indexOf(endMarker));
      });
    }
  }
});
```

- [ ] **Step 2: 테스트 실행 — 모두 통과해야 함 (Task 1~3 에서 이미 마커 삽입됨)**

```bash
cd packages/create && pnpm test markers.test.js
```
기대: 6 개 테스트 (3 파일 × 2 섹션) 모두 PASS.

- [ ] **Step 3: 안전장치 검증 — 마커가 빠지면 테스트가 실제로 깨지는지 확인**

`packages/create/templates/nextjs-standalone/src/shared/styles/tokens.css` 에서 `/* sh-ui:theme-colors-start */` 줄을 임시로 주석 처리 또는 삭제.

```bash
pnpm test markers.test.js
```
기대: 해당 테스트 FAIL. 확인 후 원상 복구.

- [ ] **Step 4: 커밋**

```bash
git add packages/create/test/markers.test.js
git commit -m "test(create): 템플릿 theme 마커 존재 검증 테스트"
```

- [ ] **Step 5: PR #1 생성 — Phase 1 완료**

`dev` 에 직접 push (이 레포 정책) 후 원격 확인.

```bash
git push origin dev
```

---

## Phase 2 — CLI 비대화형 플래그 + theme 주입 (PR #2)

### Task 5: `theme/decode.js` — base64 디코드 + 스키마 검증

**Files:**
- Create: `packages/create/src/theme/decode.js`
- Create: `packages/create/test/theme-decode.test.js`

- [ ] **Step 1: 먼저 실패하는 테스트 작성**

`packages/create/test/theme-decode.test.js`:
```javascript
import { describe, it, expect } from 'vitest';
import { decodeTheme } from '../src/theme/decode.js';

const validTheme = {
  light: {
    background: '#FFFFFF', 'background-subtle': '#FAFAFA', 'background-muted': '#F5F5F5',
    foreground: '#0A0A0A', 'foreground-muted': '#525252',
    border: '#E5E5E5', 'border-strong': '#D4D4D4',
    primary: '#171717', 'primary-foreground': '#FAFAFA', 'primary-hover': '#262626',
    danger: '#DC2626', 'danger-foreground': '#FFFFFF',
  },
  dark: {
    background: '#0A0A0A', 'background-subtle': '#171717', 'background-muted': '#262626',
    foreground: '#FAFAFA', 'foreground-muted': '#A3A3A3',
    border: '#262626', 'border-strong': '#404040',
    primary: '#FAFAFA', 'primary-foreground': '#171717', 'primary-hover': '#E5E5E5',
    danger: '#DC2626', 'danger-foreground': '#FFFFFF',
  },
  radius: 0.5,
};

const toBase64 = (obj) => Buffer.from(JSON.stringify(obj), 'utf-8').toString('base64');

describe('decodeTheme', () => {
  it('정상 base64 → theme 객체', () => {
    const b64 = toBase64(validTheme);
    expect(decodeTheme(b64)).toEqual(validTheme);
  });

  it('잘못된 base64 → 에러', () => {
    expect(() => decodeTheme('!!!not-base64!!!')).toThrow(/theme 디코드 실패/);
  });

  it('JSON 파싱 실패 → 에러', () => {
    const b64 = Buffer.from('not json', 'utf-8').toString('base64');
    expect(() => decodeTheme(b64)).toThrow(/theme 디코드 실패/);
  });

  it('light 키 누락 → 에러 메시지가 어떤 키가 빠졌는지 알려줌', () => {
    const broken = { ...validTheme, light: { ...validTheme.light } };
    delete broken.light.background;
    const b64 = toBase64(broken);
    expect(() => decodeTheme(b64)).toThrow(/light\.background/);
  });

  it('radius 타입 오류 → 에러', () => {
    const broken = { ...validTheme, radius: 'half' };
    const b64 = toBase64(broken);
    expect(() => decodeTheme(b64)).toThrow(/radius/);
  });

  it('radius 범위 초과 → 에러', () => {
    const broken = { ...validTheme, radius: 5 };
    const b64 = toBase64(broken);
    expect(() => decodeTheme(b64)).toThrow(/radius.*범위/);
  });

  it('hex 포맷 아님 → 에러', () => {
    const broken = { ...validTheme, light: { ...validTheme.light, background: 'red' } };
    const b64 = toBase64(broken);
    expect(() => decodeTheme(b64)).toThrow(/hex/);
  });
});
```

- [ ] **Step 2: 테스트 실행 — 모듈 없음으로 FAIL**

```bash
cd packages/create && pnpm test theme-decode.test.js
```
기대: FAIL — `decodeTheme` 모듈 미존재.

- [ ] **Step 3: 최소 구현 작성**

`packages/create/src/theme/decode.js`:
```javascript
const TOKEN_KEYS = [
  'background', 'background-subtle', 'background-muted',
  'foreground', 'foreground-muted',
  'border', 'border-strong',
  'primary', 'primary-foreground', 'primary-hover',
  'danger', 'danger-foreground',
];

const HEX_REGEX = /^#[0-9A-Fa-f]{6}$/;
const BASE64_REGEX = /^[A-Za-z0-9+/]*={0,2}$/;

const validateTokenMap = (name, map) => {
  if (!map || typeof map !== 'object') {
    throw new Error(`theme 디코드 실패: ${name} 가 객체가 아님`);
  }
  for (const key of TOKEN_KEYS) {
    if (!(key in map)) {
      throw new Error(`theme 디코드 실패: ${name}.${key} 누락`);
    }
    const value = map[key];
    if (typeof value !== 'string' || !HEX_REGEX.test(value)) {
      throw new Error(
        `theme 디코드 실패: ${name}.${key} 가 hex 포맷이 아님 (받은 값: ${JSON.stringify(value)})`,
      );
    }
  }
};

export const decodeTheme = (b64) => {
  if (typeof b64 !== 'string' || !BASE64_REGEX.test(b64)) {
    throw new Error(`theme 디코드 실패: base64 포맷이 아님`);
  }
  let json;
  try {
    json = Buffer.from(b64, 'base64').toString('utf-8');
  } catch (e) {
    throw new Error(`theme 디코드 실패: base64 디코드 실패 (${e.message})`);
  }
  let parsed;
  try {
    parsed = JSON.parse(json);
  } catch (e) {
    throw new Error(`theme 디코드 실패: JSON 파싱 실패 (${e.message})`);
  }
  validateTokenMap('light', parsed.light);
  validateTokenMap('dark', parsed.dark);
  if (typeof parsed.radius !== 'number' || Number.isNaN(parsed.radius)) {
    throw new Error(`theme 디코드 실패: radius 가 숫자가 아님`);
  }
  if (parsed.radius < 0 || parsed.radius > 1.5) {
    throw new Error(`theme 디코드 실패: radius 가 허용 범위(0~1.5)를 벗어남 (${parsed.radius})`);
  }
  return parsed;
};

export { TOKEN_KEYS };
```

- [ ] **Step 4: 테스트 실행 — 모두 PASS**

```bash
cd packages/create && pnpm test theme-decode.test.js
```
기대: 7 개 테스트 PASS.

- [ ] **Step 5: 커밋**

```bash
git add packages/create/src/theme/decode.js packages/create/test/theme-decode.test.js
git commit -m "feat(create): theme base64 디코드 + 스키마 검증"
```

---

### Task 6: `theme/inject.js` — 마커 블록 치환

**Files:**
- Create: `packages/create/src/theme/inject.js`
- Create: `packages/create/test/theme-inject.test.js`

섹션 이름을 받아 해당 마커 쌍 사이를 새 내용으로 교체하는 순수 함수 + 테마 값을 CSS 및 Dart 블록으로 직렬화하는 유틸.

- [ ] **Step 1: 실패하는 테스트 작성**

`packages/create/test/theme-inject.test.js`:
```javascript
import { describe, it, expect } from 'vitest';
import {
  replaceSection,
  buildCssColorsBlock,
  buildCssRadiusBlock,
  buildDartColorsBlock,
  buildDartRadiusBlock,
} from '../src/theme/inject.js';

const theme = {
  light: { background: '#111111', 'background-subtle': '#FAFAFA', 'background-muted': '#F5F5F5',
    foreground: '#0A0A0A', 'foreground-muted': '#525252',
    border: '#E5E5E5', 'border-strong': '#D4D4D4',
    primary: '#171717', 'primary-foreground': '#FAFAFA', 'primary-hover': '#262626',
    danger: '#DC2626', 'danger-foreground': '#FFFFFF',
  },
  dark: { background: '#000000', 'background-subtle': '#171717', 'background-muted': '#262626',
    foreground: '#FAFAFA', 'foreground-muted': '#A3A3A3',
    border: '#262626', 'border-strong': '#404040',
    primary: '#FAFAFA', 'primary-foreground': '#171717', 'primary-hover': '#E5E5E5',
    danger: '#DC2626', 'danger-foreground': '#FFFFFF',
  },
  radius: 0.75,
};

describe('replaceSection (CSS)', () => {
  it('마커 사이를 새 내용으로 교체', () => {
    const input = [
      '/* sh-ui:theme-colors-start */',
      ':root { --old: red; }',
      '/* sh-ui:theme-colors-end */',
      ':root { --other: blue; }',
    ].join('\n');
    const out = replaceSection(input, 'theme-colors', '/*', '*/', ':root { --new: green; }');
    expect(out).toContain(':root { --new: green; }');
    expect(out).not.toContain('--old');
    expect(out).toContain('--other: blue');
  });

  it('마커 없으면 에러', () => {
    expect(() => replaceSection('no markers', 'theme-colors', '/*', '*/', 'x'))
      .toThrow(/마커 없음/);
  });
});

describe('replaceSection (Dart)', () => {
  it('dart 스타일 마커도 처리', () => {
    const input = [
      '// sh-ui:theme-radius-start',
      'defaultRadius: 8.0,',
      '// sh-ui:theme-radius-end',
    ].join('\n');
    const out = replaceSection(input, 'theme-radius', '//', '', 'defaultRadius: 12.0,');
    expect(out).toContain('defaultRadius: 12.0,');
    expect(out).not.toContain('8.0');
  });
});

describe('buildCssColorsBlock', () => {
  it(':root + .dark 두 블록을 만듦', () => {
    const css = buildCssColorsBlock(theme);
    expect(css).toContain(':root {');
    expect(css).toContain('--background: #111111;');
    expect(css).toContain('.dark {');
    expect(css).toContain('--background: #000000;');
  });
});

describe('buildCssRadiusBlock', () => {
  it('--radius 한 줄 생성', () => {
    expect(buildCssRadiusBlock(theme)).toBe('  --radius: 0.75rem;');
  });
});

describe('buildDartColorsBlock', () => {
  it('light / dark 두 정적 상수 생성', () => {
    const dart = buildDartColorsBlock(theme);
    expect(dart).toContain('static const light = ShUiColorTokens(');
    expect(dart).toContain('background: Color(0xFF111111),');
    expect(dart).toContain('static const dark = ShUiColorTokens(');
    expect(dart).toContain('background: Color(0xFF000000),');
  });
});

describe('buildDartRadiusBlock', () => {
  it('defaultRadius 라인 생성 (px 단위)', () => {
    // 0.75rem * 16 = 12px
    expect(buildDartRadiusBlock(theme)).toContain('defaultRadius: 12.0,');
  });
});
```

- [ ] **Step 2: 테스트 실행 — FAIL (모듈 없음)**

```bash
cd packages/create && pnpm test theme-inject.test.js
```
기대: FAIL.

- [ ] **Step 3: 구현 작성**

`packages/create/src/theme/inject.js`:
```javascript
import { TOKEN_KEYS } from './decode.js';

/**
 * 파일 내용에서 sh-ui:<section>-start / -end 마커 사이 내용을 교체.
 * commentOpen / commentClose 는 파일 형식에 따라 주어짐:
 *   CSS  → '/*', '*\/'
 *   Dart → '//', ''
 */
export const replaceSection = (content, section, commentOpen, commentClose, replacement) => {
  const startMarker = commentClose
    ? `${commentOpen} sh-ui:${section}-start ${commentClose}`
    : `${commentOpen} sh-ui:${section}-start`;
  const endMarker = commentClose
    ? `${commentOpen} sh-ui:${section}-end ${commentClose}`
    : `${commentOpen} sh-ui:${section}-end`;

  const startIdx = content.indexOf(startMarker);
  const endIdx = content.indexOf(endMarker);
  if (startIdx < 0 || endIdx < 0 || endIdx < startIdx) {
    throw new Error(`inject 실패: 섹션 ${section} 마커 없음`);
  }

  const before = content.slice(0, startIdx + startMarker.length);
  const after = content.slice(endIdx);
  return `${before}\n${replacement}\n${after}`;
};

// ─── CSS 블록 빌더 ───

const cssColorLine = (key, value) => `  --${key}: ${value};`;

export const buildCssColorsBlock = (theme) => {
  const lightLines = TOKEN_KEYS.map((k) => cssColorLine(k, theme.light[k])).join('\n');
  const darkLines = TOKEN_KEYS.map((k) => cssColorLine(k, theme.dark[k])).join('\n');
  return [
    ':root {',
    lightLines,
    '}',
    '.dark {',
    darkLines,
    '}',
  ].join('\n');
};

export const buildCssRadiusBlock = (theme) => {
  return `  --radius: ${theme.radius}rem;`;
};

// ─── Dart 블록 빌더 ───

/** hex "#RRGGBB" → "Color(0xFFRRGGBB)" */
const toDartColor = (hex) => `Color(0xFF${hex.replace('#', '').toUpperCase()})`;

/** token-key → dartFieldName (backgroundSubtle 등 camelCase) */
const toCamel = (key) => key.replace(/-([a-z])/g, (_, c) => c.toUpperCase());

const dartColorLine = (key, value) => `    ${toCamel(key)}: ${toDartColor(value)},`;

export const buildDartColorsBlock = (theme) => {
  const lightLines = TOKEN_KEYS.map((k) => dartColorLine(k, theme.light[k])).join('\n');
  const darkLines = TOKEN_KEYS.map((k) => dartColorLine(k, theme.dark[k])).join('\n');
  return [
    '  static const light = ShUiColorTokens(',
    lightLines,
    '  );',
    '',
    '  static const dark = ShUiColorTokens(',
    darkLines,
    '  );',
  ].join('\n');
};

export const buildDartRadiusBlock = (theme) => {
  const px = (theme.radius * 16).toFixed(1);
  return `    defaultRadius: ${px},`;
};
```

참고: 실제 `flutter-standalone` dart 파일의 `ShUiColorTokens` 에는 `backgroundInverse`, `foregroundSubtle`, `foregroundInverse` 필드도 있지만 플레이그라운드가 편집하지 않는 값. 실제 주입은 `backgroundInverse` 등을 기본값으로 유지해야 한다 — Task 7 에서 처리.

- [ ] **Step 4: 테스트 실행 — PASS**

```bash
cd packages/create && pnpm test theme-inject.test.js
```
기대: 6 개 테스트 PASS.

- [ ] **Step 5: 커밋**

```bash
git add packages/create/src/theme/inject.js packages/create/test/theme-inject.test.js
git commit -m "feat(create): theme 마커 블록 치환 + CSS/Dart 직렬화"
```

---

### Task 7: Dart inverse/subtle 기본값 보존 처리

**Files:**
- Modify: `packages/create/src/theme/inject.js`
- Modify: `packages/create/test/theme-inject.test.js`

Playground 가 편집하는 12 개 키와 별개로, Flutter 의 `ShUiColorTokens` 는 `backgroundInverse` / `foregroundSubtle` / `foregroundInverse` 3 필드가 추가로 필요. 플레이그라운드의 기존 playground page 로직(라이트의 inverse = 다크의 self, 기타 default)을 CLI 쪽에도 재현.

- [ ] **Step 1: 테스트에 inverse / subtle 검증 추가**

`theme-inject.test.js` 의 `buildDartColorsBlock` 블록에 아래 테스트 추가:

```javascript
describe('buildDartColorsBlock — inverse/subtle', () => {
  it('light.backgroundInverse 는 dark.background 값', () => {
    const dart = buildDartColorsBlock(theme);
    // theme.dark.background 가 "#000000" 이므로
    expect(dart).toMatch(/static const light = ShUiColorTokens\([^)]*backgroundInverse: Color\(0xFF000000\)/s);
  });
  it('dark.backgroundInverse 는 light.background 값', () => {
    const dart = buildDartColorsBlock(theme);
    expect(dart).toMatch(/static const dark = ShUiColorTokens\([^)]*backgroundInverse: Color\(0xFF111111\)/s);
  });
  it('foregroundInverse 도 light↔dark 대칭', () => {
    const dart = buildDartColorsBlock(theme);
    expect(dart).toMatch(/static const light = ShUiColorTokens\([^)]*foregroundInverse: Color\(0xFFFAFAFA\)/s);
    expect(dart).toMatch(/static const dark = ShUiColorTokens\([^)]*foregroundInverse: Color\(0xFF0A0A0A\)/s);
  });
  it('foregroundSubtle 은 고정 기본값', () => {
    const dart = buildDartColorsBlock(theme);
    // light default: 0xFFA3A3A3, dark default: 0xFF737373
    expect(dart).toMatch(/static const light = ShUiColorTokens\([^)]*foregroundSubtle: Color\(0xFFA3A3A3\)/s);
    expect(dart).toMatch(/static const dark = ShUiColorTokens\([^)]*foregroundSubtle: Color\(0xFF737373\)/s);
  });
});
```

- [ ] **Step 2: 테스트 실행 — FAIL (기존 구현은 12 키만 다룸)**

```bash
cd packages/create && pnpm test theme-inject.test.js
```
기대: 방금 추가한 4 개 테스트 FAIL.

- [ ] **Step 3: `buildDartColorsBlock` 확장**

`packages/create/src/theme/inject.js` 의 dart 블록 빌더를 아래로 교체:

```javascript
/**
 * Dart 의 ShUiColorTokens 필드 순서 + 각 필드가 어떤 소스에서 값을 가져오는지.
 *   self    — 현재 모드의 편집값
 *   inverse — 반대 모드의 편집값
 *   default — playground 가 노출하지 않음, 고정 기본값 사용
 */
const DART_FIELD_SOURCES = [
  { field: 'background',           source: { kind: 'self', key: 'background' } },
  { field: 'backgroundSubtle',     source: { kind: 'self', key: 'background-subtle' } },
  { field: 'backgroundMuted',      source: { kind: 'self', key: 'background-muted' } },
  { field: 'backgroundInverse',    source: { kind: 'inverse', key: 'background' } },
  { field: 'foreground',           source: { kind: 'self', key: 'foreground' } },
  { field: 'foregroundMuted',      source: { kind: 'self', key: 'foreground-muted' } },
  { field: 'foregroundSubtle',     source: { kind: 'default' } },
  { field: 'foregroundInverse',    source: { kind: 'inverse', key: 'foreground' } },
  { field: 'border',               source: { kind: 'self', key: 'border' } },
  { field: 'borderStrong',         source: { kind: 'self', key: 'border-strong' } },
  { field: 'primary',              source: { kind: 'self', key: 'primary' } },
  { field: 'primaryForeground',    source: { kind: 'self', key: 'primary-foreground' } },
  { field: 'primaryHover',         source: { kind: 'self', key: 'primary-hover' } },
  { field: 'danger',               source: { kind: 'self', key: 'danger' } },
  { field: 'dangerForeground',     source: { kind: 'self', key: 'danger-foreground' } },
];

const DART_DEFAULTS = {
  light: { foregroundSubtle: '0xFFA3A3A3' },
  dark:  { foregroundSubtle: '0xFF737373' },
};

const buildDartStaticConst = (mode, self, opposite) => {
  const lines = DART_FIELD_SOURCES.map(({ field, source }) => {
    switch (source.kind) {
      case 'self':
        return `    ${field}: ${toDartColor(self[source.key])},`;
      case 'inverse':
        return `    ${field}: ${toDartColor(opposite[source.key])},`;
      case 'default':
        return `    ${field}: Color(${DART_DEFAULTS[mode][field]}),`;
    }
  }).join('\n');
  return [
    `  static const ${mode} = ShUiColorTokens(`,
    lines,
    '  );',
  ].join('\n');
};

export const buildDartColorsBlock = (theme) => {
  return [
    buildDartStaticConst('light', theme.light, theme.dark),
    '',
    buildDartStaticConst('dark', theme.dark, theme.light),
  ].join('\n');
};
```

기존 구 버전의 `buildDartColorsBlock` / `dartColorLine` / `toCamel` 은 제거.

- [ ] **Step 4: 테스트 실행 — 모두 PASS**

```bash
cd packages/create && pnpm test theme-inject.test.js
```
기대: 기존 + 추가 포함 모든 테스트 PASS.

- [ ] **Step 5: 커밋**

```bash
git add packages/create/src/theme/inject.js packages/create/test/theme-inject.test.js
git commit -m "feat(create): Dart inverse/subtle 필드 보존 처리"
```

---

### Task 8: `cli-args.js` — argv → flags 파서

**Files:**
- Create: `packages/create/src/cli-args.js`
- Create: `packages/create/test/cli-args.test.js`

기존 `bin/create.js` 의 argv 처리를 확장. 플래그 기반 옵션을 파싱한다.

- [ ] **Step 1: 실패하는 테스트 작성**

`packages/create/test/cli-args.test.js`:
```javascript
import { describe, it, expect } from 'vitest';
import { parseArgs } from '../src/cli-args.js';

describe('parseArgs', () => {
  it('플래그 없음 → 빈 flags', () => {
    expect(parseArgs(['node', 'create.js'])).toEqual({
      command: 'create',
      flags: {},
      positional: [],
    });
  });

  it('positional name + --platform', () => {
    const r = parseArgs(['node', 'create.js', 'my-app', '--platform', 'next']);
    expect(r.positional).toEqual(['my-app']);
    expect(r.flags.platform).toBe('next');
  });

  it('--structure / --plugins (콤마 분리) / --theme / --yes', () => {
    const r = parseArgs([
      'node', 'create.js', 'x',
      '--platform', 'next',
      '--structure', 'standalone',
      '--plugins', 'sentry,next-intl',
      '--theme', 'eyJsaWdodCI6e30=',
      '--yes',
    ]);
    expect(r.flags).toEqual({
      platform: 'next',
      structure: 'standalone',
      plugins: ['sentry', 'next-intl'],
      theme: 'eyJsaWdodCI6e30=',
      yes: true,
    });
  });

  it('add-app / add-component 서브커맨드 인식', () => {
    expect(parseArgs(['node', 'create.js', 'add-app']).command).toBe('add-app');
    expect(parseArgs(['node', 'create.js', 'add-component', 'button']).command).toBe('add-component');
  });

  it('--plugins 빈 문자열 → 빈 배열', () => {
    const r = parseArgs(['node', 'create.js', '--plugins', '']);
    expect(r.flags.plugins).toEqual([]);
  });

  it('알 수 없는 플래그 → 에러', () => {
    expect(() => parseArgs(['node', 'create.js', '--unknown', 'x']))
      .toThrow(/알 수 없는 플래그/);
  });

  it('--platform 값 누락 → 에러', () => {
    expect(() => parseArgs(['node', 'create.js', '--platform']))
      .toThrow(/--platform 값 필요/);
  });

  it('--platform 값이 잘못된 enum → 에러', () => {
    expect(() => parseArgs(['node', 'create.js', '--platform', 'django']))
      .toThrow(/--platform/);
  });
});
```

- [ ] **Step 2: 테스트 실행 — FAIL**

```bash
cd packages/create && pnpm test cli-args.test.js
```

- [ ] **Step 3: 구현**

`packages/create/src/cli-args.js`:
```javascript
const VALID_PLATFORMS = ['next', 'flutter'];
const VALID_STRUCTURES = ['standalone', 'monorepo'];
const VALID_PLUGINS = ['sentry', 'next-intl'];

const VALUE_FLAGS = ['platform', 'structure', 'plugins', 'theme'];
const BOOL_FLAGS = ['yes'];

const SUBCOMMANDS = ['add-app', 'add-component'];

export const parseArgs = (argv) => {
  const rest = argv.slice(2);
  let command = 'create';
  if (rest.length > 0 && SUBCOMMANDS.includes(rest[0])) {
    command = rest.shift();
  }

  const flags = {};
  const positional = [];

  for (let i = 0; i < rest.length; i++) {
    const arg = rest[i];
    if (!arg.startsWith('--')) {
      positional.push(arg);
      continue;
    }
    const name = arg.slice(2);
    if (BOOL_FLAGS.includes(name)) {
      flags[name] = true;
      continue;
    }
    if (!VALUE_FLAGS.includes(name)) {
      throw new Error(`알 수 없는 플래그: --${name}`);
    }
    const value = rest[i + 1];
    if (value === undefined || value.startsWith('--')) {
      throw new Error(`--${name} 값 필요`);
    }
    i++;
    if (name === 'plugins') {
      const list = value === '' ? [] : value.split(',').map((s) => s.trim()).filter(Boolean);
      for (const p of list) {
        if (!VALID_PLUGINS.includes(p)) {
          throw new Error(`알 수 없는 플러그인: ${p}`);
        }
      }
      flags.plugins = list;
      continue;
    }
    if (name === 'platform' && !VALID_PLATFORMS.includes(value)) {
      throw new Error(`--platform 은 ${VALID_PLATFORMS.join('/')} 중 하나여야 함 (받은 값: ${value})`);
    }
    if (name === 'structure' && !VALID_STRUCTURES.includes(value)) {
      throw new Error(`--structure 는 ${VALID_STRUCTURES.join('/')} 중 하나여야 함 (받은 값: ${value})`);
    }
    flags[name] = value;
  }

  return { command, flags, positional };
};
```

- [ ] **Step 4: 테스트 실행 — PASS**

```bash
cd packages/create && pnpm test cli-args.test.js
```

- [ ] **Step 5: 커밋**

```bash
git add packages/create/src/cli-args.js packages/create/test/cli-args.test.js
git commit -m "feat(create): CLI argv → flags 파서"
```

---

### Task 9: `generator.js` — flags 수용 + inquirer 스킵 로직

**Files:**
- Modify: `packages/create/src/generator.js`
- Modify: `packages/create/test/smoke.test.js`

`createProject` 시그니처를 `createProject(options = {})` 로 바꿔, 플래그로 들어온 항목은 inquirer 프롬프트를 우회.

- [ ] **Step 1: 새 시나리오 — 플래그만으로 완전 비대화형 실행 성공** (smoke 테스트에 추가)

`packages/create/test/smoke.test.js` 에 기존 시나리오 뒤에 추가:

```javascript
  it('scenario 6 — 모든 플래그 (inquirer 호출 없음)', async () => {
    // prompts 는 mock 된 상태 — 아무 mockResolvedValue 도 세팅 안 함
    await createProject({
      name: 'flaggy',
      platform: 'next',
      structure: 'standalone',
      plugins: ['sentry'],
      yes: true,
    });

    const projectDir = path.join(tmpDir, 'flaggy');
    expect(await fs.pathExists(projectDir)).toBe(true);
    const pkg = await fs.readJson(path.join(projectDir, 'package.json'));
    expect(pkg.name).toBe('flaggy');
    expect(pkg.dependencies['@sentry/nextjs']).toBeDefined();

    // inquirer 는 한 번도 호출되지 않았어야 함
    expect(prompts.input).not.toHaveBeenCalled();
    expect(prompts.select).not.toHaveBeenCalled();
    expect(prompts.checkbox).not.toHaveBeenCalled();
  });

  it('scenario 7 — 부분 플래그 (name, platform 만 제공, 나머지는 프롬프트)', async () => {
    prompts.select.mockResolvedValueOnce('standalone'); // structure 만 물음
    prompts.checkbox.mockResolvedValueOnce([]);          // plugins

    await createProject({
      name: 'partial',
      platform: 'next',
    });

    const projectDir = path.join(tmpDir, 'partial');
    expect(await fs.pathExists(projectDir)).toBe(true);

    // name / platform 은 프롬프트 우회
    expect(prompts.input).not.toHaveBeenCalled();
    // structure 는 프롬프트 호출됨 (select 1 번)
    expect(prompts.select).toHaveBeenCalledTimes(1);
  });
```

- [ ] **Step 2: 테스트 실행 — FAIL (시그니처 미구현)**

```bash
cd packages/create && pnpm test smoke.test.js
```

- [ ] **Step 3: `generator.js` 의 `createProject` 시그니처 변경**

`packages/create/src/generator.js` 상단에 import 추가 + `createProject` 함수 교체:

```javascript
import { input, select, checkbox, confirm } from '@inquirer/prompts';
import { execSync } from 'node:child_process';
import fs from 'fs-extra';
import path from 'node:path';
import { fileURLToPath } from 'node:url';
import { getPluginChoices, getPluginsByNames } from './plugins/index.js';

const __dirname = path.dirname(fileURLToPath(import.meta.url));
const TEMPLATES_DIR = path.join(__dirname, '..', 'templates');

// ─── Create new project ───

export async function createProject(options = {}) {
  const projectName = options.name ?? await input({
    message: '프로젝트 이름:',
    default: 'my-app',
  });

  const platform = options.platform ?? await select({
    message: '플랫폼:',
    choices: [
      { name: 'Next.js', value: 'next' },
      { name: 'Flutter', value: 'flutter' },
    ],
  });

  const targetDir = path.resolve(process.cwd(), projectName);

  if (await fs.pathExists(targetDir)) {
    if (options.yes) {
      await fs.remove(targetDir);
    } else {
      const overwrite = await confirm({
        message: `${projectName} 디렉토리가 이미 존재합니다. 덮어쓸까요?`,
        default: false,
      });
      if (!overwrite) {
        console.log('취소되었습니다.');
        return;
      }
      await fs.remove(targetDir);
    }
  }

  if (platform === 'flutter') {
    await generateFlutter(targetDir, projectName);
    console.log(`\n✅ ${projectName} Flutter 프로젝트가 생성되었습니다!`);
    console.log(`\n  cd ${projectName}`);
    console.log('  flutter pub get');
    console.log('  flutter run\n');
    return;
  }

  // platform === 'next' 경로
  const projectType = options.structure ?? await select({
    message: '프로젝트 구조:',
    choices: [
      { name: '단독 (Next.js standalone)', value: 'standalone' },
      { name: '모노레포 (Turborepo + pnpm)', value: 'monorepo' },
    ],
  });

  const selectedPluginNames = options.plugins ?? await checkbox({
    message: '추가 기능 선택 (Space로 선택):',
    choices: getPluginChoices(),
  });

  const plugins = getPluginsByNames(selectedPluginNames);
  plugins.sort((a, b) => (a.priority ?? 0) - (b.priority ?? 0));

  if (projectType === 'standalone') {
    await generateStandalone(targetDir, projectName, plugins);
  } else {
    await generateMonorepo(targetDir, projectName, plugins, { yes: options.yes });
  }

  console.log(`\n✅ ${projectName} 프로젝트가 생성되었습니다!`);
  console.log(`\n  cd ${projectName}`);
  console.log('  pnpm install');
  console.log('  pnpm dev\n');
}
```

참고: `generateMonorepo` 가 내부에서 추가로 input 을 호출함(첫 앱 이름·포트). 플래그 기반 모드에서 기본값을 쓰도록 `{ yes }` 옵션을 전달해 모노레포 쪽도 비대화형 기본값(`web` / `3000`)을 쓰게 한다. Step 4 에서 처리.

- [ ] **Step 4: `generateMonorepo` 가 `yes` 플래그 받을 때 내부 프롬프트 스킵**

`generator.js` 의 `generateMonorepo` 함수 시그니처와 앱 생성 부분 수정:

```javascript
async function generateMonorepo(targetDir, projectName, plugins, { yes = false } = {}) {
  await fs.copy(path.join(TEMPLATES_DIR, 'monorepo'), targetDir);

  const rootPkgPath = path.join(targetDir, 'package.json');
  const rootPkg = await fs.readJson(rootPkgPath);
  rootPkg.name = projectName;
  await fs.writeJson(rootPkgPath, rootPkg, { spaces: 2 });

  const turboPath = path.join(targetDir, 'turbo.json');
  const turbo = await fs.readJson(turboPath);
  for (const plugin of plugins) {
    if (plugin.turboEnvVars) {
      turbo.globalEnv.push(...plugin.turboEnvVars);
    }
  }
  await fs.writeJson(turboPath, turbo, { spaces: 2 });

  const appName = yes ? 'web' : await input({
    message: '첫 번째 앱 이름:',
    default: 'web',
  });
  const port = yes ? '3000' : await input({
    message: '포트 번호:',
    default: '3000',
  });

  const appsDir = path.join(targetDir, 'apps', appName);
  await generateApp(appsDir, appName, port, plugins);
}
```

(기존 함수 내 해당 2 줄 `const appName = await input(...)` 와 `const port = await input(...)` 를 `yes ? ... : await input(...)` 로 교체.)

- [ ] **Step 5: 테스트 실행 — 모든 시나리오 PASS**

```bash
cd packages/create && pnpm test smoke.test.js
```
기대: scenario 1~7 모두 PASS.

- [ ] **Step 6: 커밋**

```bash
git add packages/create/src/generator.js packages/create/test/smoke.test.js
git commit -m "feat(create): createProject(options) — flags 기반 비대화형 지원"
```

---

### Task 10: `generator.js` — theme 주입 훅

**Files:**
- Modify: `packages/create/src/generator.js`
- Modify: `packages/create/test/smoke.test.js`

각 템플릿 생성 직후 `theme/inject.js` 를 호출해 tokens 파일을 교체.

- [ ] **Step 1: 실패하는 테스트 추가**

`smoke.test.js` 끝에 추가 (파일 위쪽에 import 먼저 추가):

```javascript
// 파일 상단 import 에 추가:
import { TOKEN_KEYS } from '../src/theme/decode.js';

// ... (기존 시나리오들) ...

  it('scenario 8 — theme 주입 (Next.js standalone)', async () => {
    // custom theme: background 를 빨강으로, radius 를 1rem 으로
    const red = Object.fromEntries(TOKEN_KEYS.map((k) => [k, '#FF0000']));
    const blue = Object.fromEntries(TOKEN_KEYS.map((k) => [k, '#0000FF']));
    const theme = { light: red, dark: blue, radius: 1 };
    const themeB64 = Buffer.from(JSON.stringify(theme), 'utf-8').toString('base64');

    await createProject({
      name: 'themed',
      platform: 'next',
      structure: 'standalone',
      plugins: [],
      theme: themeB64,
      yes: true,
    });

    const cssPath = path.join(tmpDir, 'themed', 'src', 'shared', 'styles', 'tokens.css');
    const css = await fs.readFile(cssPath, 'utf-8');

    // :root 안에 custom light 색이 들어갔는지
    expect(css).toMatch(/:root\s*\{[^}]*--background:\s*#FF0000/s);
    // .dark 안에 custom dark 색이 들어갔는지
    expect(css).toMatch(/\.dark\s*\{[^}]*--background:\s*#0000FF/s);
    // radius
    expect(css).toContain('--radius: 1rem;');
    // 마커 바깥 토큰은 그대로 유지
    expect(css).toContain('--space-0: 0px;');
  });

  it('scenario 9 — theme 주입 (Flutter)', async () => {
    const red = Object.fromEntries(TOKEN_KEYS.map((k) => [k, '#FF0000']));
    const blue = Object.fromEntries(TOKEN_KEYS.map((k) => [k, '#0000FF']));
    const theme = { light: red, dark: blue, radius: 0.75 };
    const themeB64 = Buffer.from(JSON.stringify(theme), 'utf-8').toString('base64');

    await createProject({
      name: 'flutter-themed',
      platform: 'flutter',
      theme: themeB64,
      yes: true,
    });

    const dartPath = path.join(
      tmpDir, 'flutter-themed', 'lib', 'sh_ui', 'foundation', 'sh_ui_tokens.dart',
    );
    const dart = await fs.readFile(dartPath, 'utf-8');

    expect(dart).toMatch(/static const light = ShUiColorTokens\([^)]*background: Color\(0xFFFF0000\)/s);
    expect(dart).toMatch(/static const dark = ShUiColorTokens\([^)]*background: Color\(0xFF0000FF\)/s);
    // 0.75rem * 16 = 12px
    expect(dart).toContain('defaultRadius: 12.0,');
    // spacing 는 그대로
    expect(dart).toContain('class ShUiSpacingTokens');
  });

  it('scenario 10 — theme 없음: 템플릿 기본값 유지', async () => {
    await createProject({
      name: 'no-theme',
      platform: 'next',
      structure: 'standalone',
      plugins: [],
      yes: true,
    });

    const cssPath = path.join(tmpDir, 'no-theme', 'src', 'shared', 'styles', 'tokens.css');
    const css = await fs.readFile(cssPath, 'utf-8');
    expect(css).toContain('--background: #FFFFFF;'); // 템플릿 기본
    expect(css).toContain('--radius: 0.5rem;');
  });

  it('scenario 11 — 잘못된 theme base64 → 에러', async () => {
    await expect(
      createProject({
        name: 'bad-theme',
        platform: 'next',
        structure: 'standalone',
        plugins: [],
        theme: 'not-valid-base64!!!',
        yes: true,
      }),
    ).rejects.toThrow(/theme 디코드 실패/);
  });
```

- [ ] **Step 2: 테스트 실행 — FAIL (theme 주입 미구현)**

```bash
cd packages/create && pnpm test smoke.test.js
```

- [ ] **Step 3: `generator.js` 에 theme 주입 추가**

상단 import:
```javascript
import { decodeTheme } from './theme/decode.js';
import {
  replaceSection,
  buildCssColorsBlock,
  buildCssRadiusBlock,
  buildDartColorsBlock,
  buildDartRadiusBlock,
} from './theme/inject.js';
```

`createProject` 의 플랫폼 분기 전에 `theme` 디코드:

```javascript
  // options.theme 가 있으면 먼저 디코드(검증)해서 뒤에서 주입
  const theme = options.theme ? decodeTheme(options.theme) : null;
```

위치: `const targetDir = path.resolve(...)` 줄 바로 위.

주입 헬퍼 함수들을 `generator.js` 끝에 추가:

```javascript
// ─── Theme 주입 ───

/** 여러 후보 경로 중 존재하는 첫 tokens.css 에 theme 주입 */
async function injectCssTheme(projectDir, theme) {
  if (!theme) return;
  const candidates = [
    'src/shared/styles/tokens.css',
    'src/styles/tokens.css',
  ];
  for (const rel of candidates) {
    const abs = path.join(projectDir, rel);
    if (await fs.pathExists(abs)) {
      let css = await fs.readFile(abs, 'utf-8');
      css = replaceSection(css, 'theme-colors', '/*', '*/', buildCssColorsBlock(theme));
      css = replaceSection(css, 'theme-radius', '/*', '*/', buildCssRadiusBlock(theme));
      await fs.writeFile(abs, css);
      return;
    }
  }
  throw new Error(`theme 주입 실패: tokens.css 파일을 찾을 수 없음 (${projectDir})`);
}

async function injectDartTheme(projectDir, theme) {
  if (!theme) return;
  const abs = path.join(projectDir, 'lib/sh_ui/foundation/sh_ui_tokens.dart');
  if (!(await fs.pathExists(abs))) {
    throw new Error(`theme 주입 실패: sh_ui_tokens.dart 가 없음 (${abs})`);
  }
  let dart = await fs.readFile(abs, 'utf-8');
  dart = replaceSection(dart, 'theme-colors', '//', '', buildDartColorsBlock(theme));
  dart = replaceSection(dart, 'theme-radius', '//', '', buildDartRadiusBlock(theme));
  await fs.writeFile(abs, dart);
}
```

`generateFlutter` 에 theme 파라미터 전달 + 주입 호출:

```javascript
async function generateFlutter(targetDir, projectName, theme) {
  await fs.copy(path.join(TEMPLATES_DIR, 'flutter-standalone'), targetDir);
  await replaceInAllFiles(targetDir, '{{project_name}}', projectName);
  await injectDartTheme(targetDir, theme);
}
```

`generateStandalone` 에 theme 파라미터 전달 + 주입 호출 (함수 끝에):

```javascript
async function generateStandalone(targetDir, projectName, plugins, theme) {
  // ...기존 로직 그대로...
  await applyTransforms(targetDir, plugins);
  await injectCssTheme(targetDir, theme);
}
```

`generateMonorepo` 도 동일하게 (모노레포는 앱 생성 후 앱 디렉토리에 주입):

```javascript
async function generateMonorepo(targetDir, projectName, plugins, { yes = false, theme } = {}) {
  // ...기존 로직...

  const appsDir = path.join(targetDir, 'apps', appName);
  await generateApp(appsDir, appName, port, plugins);

  // 앱의 tokens.css 에 주입 (packages/ui/ui-apps/ui-<app>/src/styles/tokens.css)
  const uiAppDir = path.join(targetDir, 'packages', 'ui', 'ui-apps', `ui-${appName}`);
  await injectCssTheme(uiAppDir, theme);
}
```

`createProject` 의 플랫폼 분기를 아래처럼 theme 전달하도록 수정:

```javascript
  if (platform === 'flutter') {
    await generateFlutter(targetDir, projectName, theme);
    console.log(...);
    return;
  }

  // ...projectType 결정...

  if (projectType === 'standalone') {
    await generateStandalone(targetDir, projectName, plugins, theme);
  } else {
    await generateMonorepo(targetDir, projectName, plugins, { yes: options.yes, theme });
  }
```

- [ ] **Step 4: 테스트 실행 — PASS**

```bash
cd packages/create && pnpm test smoke.test.js
```
기대: scenario 1~11 모두 PASS.

- [ ] **Step 5: 커밋**

```bash
git add packages/create/src/generator.js packages/create/test/smoke.test.js
git commit -m "feat(create): --theme 플래그로 토큰 주입"
```

---

### Task 11: `bin/create.js` — cli-args 통합

**Files:**
- Modify: `packages/create/bin/create.js`

argv 파싱을 `parseArgs` 로 옮기고 `createProject` / `addApp` / `addComponent` 에 flags 전달.

- [ ] **Step 1: 파일을 아래 내용으로 교체**

```javascript
#!/usr/bin/env node

import { argv, exit } from 'node:process';
import { parseArgs } from '../src/cli-args.js';
import { createProject, addApp, addComponent } from '../src/generator.js';

let parsed;
try {
  parsed = parseArgs(argv);
} catch (e) {
  console.error(`❌ ${e.message}`);
  exit(1);
}

const { command, flags, positional } = parsed;

try {
  if (command === 'add-app') {
    await addApp();
  } else if (command === 'add-component') {
    const componentName = positional[0];
    await addComponent(componentName, flags.app);
  } else {
    await createProject({
      name: positional[0],
      platform: flags.platform,
      structure: flags.structure,
      plugins: flags.plugins,
      theme: flags.theme,
      yes: flags.yes,
    });
  }
} catch (e) {
  console.error(`❌ ${e.message}`);
  exit(1);
}
```

참고: 기존 `--app` 플래그(`add-component --app <name>`) 처리는 `cli-args.js` 에 `app` 을 `VALUE_FLAGS` 에 추가해야 함.

- [ ] **Step 2: `cli-args.js` 의 `VALUE_FLAGS` 에 `'app'` 추가**

```javascript
const VALUE_FLAGS = ['platform', 'structure', 'plugins', 'theme', 'app'];
```

- [ ] **Step 3: 전체 테스트 재실행 — 회귀 없음 확인**

```bash
cd packages/create && pnpm test
```
기대: 모든 테스트 (markers + theme-decode + theme-inject + cli-args + smoke) PASS.

- [ ] **Step 4: 커밋**

```bash
git add packages/create/bin/create.js packages/create/src/cli-args.js
git commit -m "feat(create): bin 에 cli-args 통합 + --app 플래그 지원"
```

---

### Task 12: 수동 검증 — 실제 CLI 실행

**Files:**
- 없음 (수동)

- [ ] **Step 1: 로컬에서 CLI 직접 실행**

```bash
cd /tmp
rm -rf /tmp/sh-ui-test-*
node /Users/gimsanghyeon/development/PROJECT/sh-ui/packages/create/bin/create.js sh-ui-test-1 \
  --platform next --structure standalone --plugins sentry --yes
```
기대: `/tmp/sh-ui-test-1/` 생성, `@sentry/nextjs` 가 `package.json` 에 추가.

- [ ] **Step 2: theme 플래그 포함 실행**

```bash
THEME=$(node -e "const k=['background','background-subtle','background-muted','foreground','foreground-muted','border','border-strong','primary','primary-foreground','primary-hover','danger','danger-foreground']; const red=Object.fromEntries(k.map(x=>[x,'#FF0000'])); console.log(Buffer.from(JSON.stringify({light:red,dark:red,radius:1})).toString('base64'))")

node /Users/gimsanghyeon/development/PROJECT/sh-ui/packages/create/bin/create.js sh-ui-test-2 \
  --platform next --structure standalone --plugins '' --theme "$THEME" --yes

grep -A2 '^:root' /tmp/sh-ui-test-2/src/shared/styles/tokens.css | head -5
```
기대: `--background: #FF0000;` 등 빨간색 토큰이 출력됨.

- [ ] **Step 3: 에러 케이스 — 잘못된 theme**

```bash
node /Users/gimsanghyeon/development/PROJECT/sh-ui/packages/create/bin/create.js bad \
  --platform next --structure standalone --plugins '' --theme 'garbage!!' --yes
```
기대: `❌ theme 디코드 실패: ...` 출력 후 exit 1.

- [ ] **Step 4: 임시 생성물 정리**

```bash
rm -rf /tmp/sh-ui-test-1 /tmp/sh-ui-test-2
```

---

### Task 13: `@sh-ui/create` MINOR 범프 + versions.json + 릴리즈

**Files:**
- Modify: `packages/create/package.json`
- Modify: `packages/changelog/versions.json`

- [ ] **Step 1: 현재 버전 확인**

```bash
node -e "console.log(require('/Users/gimsanghyeon/development/PROJECT/sh-ui/packages/create/package.json').version)"
```
기대: `0.17.0` (이후 값이면 그 값 + MINOR).

- [ ] **Step 2: package.json version 범프 — MINOR → `0.18.0`**

`packages/create/package.json`:
```diff
-  "version": "0.17.0",
+  "version": "0.18.0",
```

- [ ] **Step 3: `versions.json` 에 엔트리 prepend**

`packages/changelog/versions.json` 의 `versions` 배열 맨 앞에 추가:

```json
{
  "version": "0.18.0",
  "date": "2026-04-22",
  "title": "@sh-ui/create — 비대화형 플래그와 theme 주입",
  "type": "minor",
  "highlights": [
    "--platform / --structure / --plugins / --theme / --yes 플래그 지원 — 완전 비대화형 실행 가능",
    "--theme <base64> 로 커스텀 색·radius 를 프로젝트 생성 시점에 바로 주입 (플레이그라운드 연동 준비)",
    "템플릿의 tokens.css / sh_ui_tokens.dart 에 theme 섹션 마커 삽입 — 토큰만 교체되고 다른 값(spacing 등)은 유지"
  ],
  "url": "https://github.com/sanghyeonKim0201/sh-ui/releases/tag/v0.18.0"
}
```

- [ ] **Step 4: 전체 테스트 통과 최종 확인**

```bash
cd /Users/gimsanghyeon/development/PROJECT/sh-ui && pnpm -r test
```
기대: 모든 패키지 테스트 PASS.

- [ ] **Step 5: 한 커밋으로 묶기**

```bash
cd /Users/gimsanghyeon/development/PROJECT/sh-ui
git add packages/create/package.json packages/changelog/versions.json
git commit -m "chore(create): v0.18.0 — 비대화형 플래그 + theme 주입"
```

- [ ] **Step 6: push + 태그 + 릴리즈**

```bash
git push origin dev
git tag v0.18.0
git push origin v0.18.0
gh release create v0.18.0 --title "v0.18.0 — @sh-ui/create 비대화형 플래그 + theme 주입" --notes "$(cat <<'EOF'
## 새 기능

- `--platform <next|flutter>` / `--structure <standalone|monorepo>` / `--plugins <...>` / `--yes` 플래그로 완전 비대화형 실행 가능
- `--theme <base64>` 로 색·radius 를 프로젝트 생성 시점에 바로 주입 (플레이그라운드와의 연동은 다음 릴리즈에서)

## 내부 변경

- 템플릿의 `tokens.css` / `sh_ui_tokens.dart` 에 `sh-ui:theme-colors-start/end` 와 `sh-ui:theme-radius-start/end` 마커 삽입 — theme 만 교체되고 spacing/text/shadow 등은 유지
- smoke.test 에 theme 주입 E2E 시나리오 추가

## 하위 호환

- 플래그 없이 실행하면 기존 inquirer 대화형 흐름 그대로
EOF
)"
```

---

## Phase 3 — Playground FAB + Dialog (PR #3)

### Task 14: `encodeTheme.ts` — playground 상태 → base64

**Files:**
- Create: `apps/docs/components/playground/encodeTheme.ts`

순수 함수 1 개. 테스트는 수동 (docs 앱에 vitest 없음).

- [ ] **Step 1: 파일 작성**

```typescript
type TokenKey =
  | "background" | "background-subtle" | "background-muted"
  | "foreground" | "foreground-muted"
  | "border" | "border-strong"
  | "primary" | "primary-foreground" | "primary-hover"
  | "danger" | "danger-foreground";

type Mode = "light" | "dark";

export type ThemeConfig = {
  light: Record<TokenKey, string>;
  dark: Record<TokenKey, string>;
  radius: number;
};

export const encodeTheme = (cfg: ThemeConfig): string => {
  const json = JSON.stringify(cfg);
  // btoa 는 ASCII-only. hex + 숫자만 있으니 안전.
  return btoa(json);
};

export type { TokenKey, Mode };
```

- [ ] **Step 2: 수동 검증 — 콘솔에서 round-trip 확인**

(아직 import 하는 곳이 없어 컴파일만 확인)

```bash
cd /Users/gimsanghyeon/development/PROJECT/sh-ui/apps/docs && pnpm typecheck
```
기대: 에러 없음.

- [ ] **Step 3: 커밋**

```bash
git add apps/docs/components/playground/encodeTheme.ts
git commit -m "feat(docs): playground theme base64 인코더"
```

---

### Task 15: `useCommandComposer.ts` — flags → 명령어 문자열

**Files:**
- Create: `apps/docs/components/playground/useCommandComposer.ts`

- [ ] **Step 1: 파일 작성**

```typescript
import { useMemo } from "react";

export type PackageManager = "pnpm" | "npm" | "yarn" | "bun";
export type Platform = "next" | "flutter";
export type Structure = "standalone" | "monorepo";
export type Plugin = "sentry" | "next-intl";

export type ComposerOptions = {
  projectName: string;
  platform: Platform;
  structure: Structure;
  plugins: ReadonlySet<Plugin>;
  packageManager: PackageManager;
  themeBase64: string;
};

const packageManagerPrefix: Record<PackageManager, string> = {
  pnpm: "pnpm dlx @sh-ui/create",
  npm: "npx @sh-ui/create",
  yarn: "yarn dlx @sh-ui/create",
  bun: "bunx @sh-ui/create",
};

export const composeCommand = (opts: ComposerOptions): string => {
  const { projectName, platform, structure, plugins, packageManager, themeBase64 } = opts;

  const parts = [packageManagerPrefix[packageManager], projectName];
  parts.push("--platform", platform);

  if (platform === "next") {
    parts.push("--structure", structure);
    const pluginsArg = [...plugins].join(",");
    parts.push("--plugins", pluginsArg || "''");
  }

  parts.push("--theme", `'${themeBase64}'`);
  parts.push("--yes");

  return parts.join(" ");
};

export const useCommandComposer = (opts: ComposerOptions): string =>
  useMemo(() => composeCommand(opts), [opts]);
```

- [ ] **Step 2: 타입 체크**

```bash
cd /Users/gimsanghyeon/development/PROJECT/sh-ui/apps/docs && pnpm typecheck
```

- [ ] **Step 3: 커밋**

```bash
git add apps/docs/components/playground/useCommandComposer.ts
git commit -m "feat(docs): create-project 명령어 컴포저"
```

---

### Task 16: `CreateProjectDialog.tsx` — 다이얼로그 UI

**Files:**
- Create: `apps/docs/components/playground/CreateProjectDialog.tsx`

- [ ] **Step 1: 파일 작성**

```tsx
"use client";

import { useMemo, useState } from "react";
import {
  Dialog,
  DialogContent,
  DialogTitle,
  DialogDescription,
  DialogClose,
  DialogFooter,
} from "@/components/ui/dialog";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { ToggleGroup, ToggleGroupItem } from "@/components/ui/toggle";
import { Tabs, TabsList, TabsTrigger, TabsContent } from "@/components/ui/tabs";
import {
  composeCommand,
  type ComposerOptions,
  type PackageManager,
  type Platform,
  type Plugin,
  type Structure,
} from "./useCommandComposer";
import { encodeTheme, type TokenKey, type Mode, type ThemeConfig } from "./encodeTheme";

type Props = {
  open: boolean;
  onClose: () => void;
  light: Record<TokenKey, string>;
  dark: Record<TokenKey, string>;
  radius: number;
  mode: Mode;
};

const SWATCH_KEYS: TokenKey[] = ["background", "foreground", "primary", "danger"];

export function CreateProjectDialog({ open, onClose, light, dark, radius, mode }: Props) {
  const [projectName, setProjectName] = useState("my-app");
  const [platform, setPlatform] = useState<Platform>("next");
  const [structure, setStructure] = useState<Structure>("standalone");
  const [plugins, setPlugins] = useState<Set<Plugin>>(new Set());
  const [packageManager, setPackageManager] = useState<PackageManager>("pnpm");
  const [copied, setCopied] = useState(false);

  const theme: ThemeConfig = useMemo(() => ({ light, dark, radius }), [light, dark, radius]);
  const themeBase64 = useMemo(() => encodeTheme(theme), [theme]);

  const command = useMemo(
    () =>
      composeCommand({
        projectName: projectName || "my-app",
        platform,
        structure,
        plugins,
        packageManager,
        themeBase64,
      } as ComposerOptions),
    [projectName, platform, structure, plugins, packageManager, themeBase64],
  );

  const togglePlugin = (p: Plugin) => {
    setPlugins((prev) => {
      const next = new Set(prev);
      if (next.has(p)) next.delete(p);
      else next.add(p);
      return next;
    });
  };

  const handleCopy = async () => {
    try {
      await navigator.clipboard.writeText(command);
      setCopied(true);
      setTimeout(() => setCopied(false), 1800);
    } catch {
      // fallback: 사용자에게 수동 선택 유도 — 이미 <pre> 로 노출되므로 그 외 처리 없음
    }
  };

  const swatchSource = mode === "light" ? light : dark;

  return (
    <Dialog open={open} onOpenChange={(v) => { if (!v) onClose(); }}>
      <DialogContent style={{ maxWidth: 440, width: "100%" }}>
        <DialogTitle>프로젝트 만들기</DialogTitle>
        <DialogDescription>편집한 디자인이 그대로 적용됩니다.</DialogDescription>

        <div style={{ display: "flex", flexDirection: "column", gap: "0.875rem", marginTop: "0.5rem" }}>
          {/* theme summary */}
          <div
            style={{
              display: "flex",
              alignItems: "center",
              gap: "0.5rem",
              padding: "0.5rem 0.625rem",
              background: "var(--background-subtle)",
              border: "1px solid var(--border)",
              borderRadius: "calc(var(--radius) - 2px)",
              fontSize: "0.75rem",
            }}
          >
            <div style={{ display: "flex", gap: 2 }}>
              {SWATCH_KEYS.map((k) => (
                <div
                  key={k}
                  aria-hidden
                  style={{
                    width: 14,
                    height: 14,
                    borderRadius: 2,
                    background: swatchSource[k],
                    border: "1px solid var(--border)",
                  }}
                />
              ))}
            </div>
            <span style={{ color: "var(--foreground-muted)" }}>
              현재 토큰 · radius {radius.toFixed(2)}rem
            </span>
          </div>

          {/* project name */}
          <div>
            <Label htmlFor="create-name">프로젝트 이름</Label>
            <Input
              id="create-name"
              value={projectName}
              onChange={(e) => setProjectName(e.target.value)}
              placeholder="my-app"
            />
          </div>

          {/* platform */}
          <div>
            <Label>플랫폼</Label>
            <ToggleGroup value={[platform]} onValueChange={(v: string[]) => v[0] && setPlatform(v[0] as Platform)}>
              <ToggleGroupItem value="next">Next.js</ToggleGroupItem>
              <ToggleGroupItem value="flutter">Flutter</ToggleGroupItem>
            </ToggleGroup>
          </div>

          {/* next-only */}
          {platform === "next" && (
            <>
              <div>
                <Label>구조</Label>
                <ToggleGroup value={[structure]} onValueChange={(v: string[]) => v[0] && setStructure(v[0] as Structure)}>
                  <ToggleGroupItem value="standalone">Standalone</ToggleGroupItem>
                  <ToggleGroupItem value="monorepo">Monorepo</ToggleGroupItem>
                </ToggleGroup>
              </div>
              <div>
                <Label>플러그인</Label>
                <div style={{ display: "flex", gap: "0.375rem", flexWrap: "wrap" }}>
                  <Button
                    type="button"
                    variant={plugins.has("sentry") ? "primary" : "secondary"}
                    size="sm"
                    onClick={() => togglePlugin("sentry")}
                  >
                    {plugins.has("sentry") ? "✓ " : ""}Sentry
                  </Button>
                  <Button
                    type="button"
                    variant={plugins.has("next-intl") ? "primary" : "secondary"}
                    size="sm"
                    onClick={() => togglePlugin("next-intl")}
                  >
                    {plugins.has("next-intl") ? "✓ " : ""}next-intl
                  </Button>
                </div>
              </div>
            </>
          )}

          {/* package manager + command preview */}
          <div>
            <Tabs value={packageManager} onValueChange={(v) => setPackageManager(v as PackageManager)}>
              <TabsList>
                <TabsTrigger value="pnpm">pnpm</TabsTrigger>
                <TabsTrigger value="npm">npm</TabsTrigger>
                <TabsTrigger value="yarn">yarn</TabsTrigger>
                <TabsTrigger value="bun">bun</TabsTrigger>
              </TabsList>
              <TabsContent value={packageManager}>
                <pre
                  style={{
                    margin: 0,
                    padding: "0.625rem 0.75rem",
                    background: "var(--background-subtle)",
                    border: "1px solid var(--border)",
                    borderRadius: "calc(var(--radius) - 2px)",
                    fontSize: "0.75rem",
                    fontFamily: "ui-monospace, SFMono-Regular, Menlo, Consolas, monospace",
                    overflowX: "auto",
                    whiteSpace: "pre",
                    color: "var(--foreground)",
                  }}
                >
                  {command}
                </pre>
              </TabsContent>
            </Tabs>
          </div>

          {/* aria-live for copy feedback */}
          <div role="status" aria-live="polite" style={{ position: "absolute", left: -10000 }}>
            {copied ? "명령어가 복사되었습니다" : ""}
          </div>
        </div>

        <DialogFooter>
          <DialogClose render={<Button variant="secondary">닫기</Button>} />
          <Button onClick={handleCopy}>{copied ? "복사됨" : "명령어 복사"}</Button>
        </DialogFooter>
      </DialogContent>
    </Dialog>
  );
}
```

- [ ] **Step 2: 타입 체크**

```bash
cd /Users/gimsanghyeon/development/PROJECT/sh-ui/apps/docs && pnpm typecheck
```
기대: 에러 없음. (Dialog / Button / Input / Label / ToggleGroup / Tabs 모두 이미 `components/ui/` 에 존재 — Task 프리체크에서 확인 완료.)

- [ ] **Step 3: 커밋**

```bash
git add apps/docs/components/playground/CreateProjectDialog.tsx
git commit -m "feat(docs): 플레이그라운드 create-project 다이얼로그"
```

---

### Task 17: `CreateProjectFab.tsx` — 우하단 FAB

**Files:**
- Create: `apps/docs/components/playground/CreateProjectFab.tsx`

- [ ] **Step 1: 파일 작성**

```tsx
"use client";

type Props = {
  onClick: () => void;
};

export function CreateProjectFab({ onClick }: Props) {
  return (
    <button
      type="button"
      onClick={onClick}
      aria-label="프로젝트 만들기"
      style={{
        position: "fixed",
        bottom: "1.5rem",
        right: "1.5rem",
        zIndex: 500,
        display: "inline-flex",
        alignItems: "center",
        gap: "0.5rem",
        padding: "0.625rem 1rem",
        background: "var(--primary)",
        color: "var(--primary-foreground)",
        border: "none",
        borderRadius: "999px",
        fontSize: "0.8125rem",
        fontWeight: 500,
        cursor: "pointer",
        boxShadow: "var(--shadow-lg)",
      }}
    >
      <span aria-hidden>＋</span>
      <span>프로젝트 만들기</span>
    </button>
  );
}
```

- [ ] **Step 2: 타입 체크**

```bash
cd /Users/gimsanghyeon/development/PROJECT/sh-ui/apps/docs && pnpm typecheck
```

- [ ] **Step 3: 커밋**

```bash
git add apps/docs/components/playground/CreateProjectFab.tsx
git commit -m "feat(docs): 플레이그라운드 FAB"
```

---

### Task 18: `page.tsx` 통합

**Files:**
- Modify: `apps/docs/app/playground/page.tsx`

- [ ] **Step 1: import 추가**

`page.tsx` 상단 import 섹션 끝에:
```tsx
import { CreateProjectFab } from "@/components/playground/CreateProjectFab";
import { CreateProjectDialog } from "@/components/playground/CreateProjectDialog";
```

- [ ] **Step 2: `PlaygroundPage` 안에 open 상태 + 컴포넌트 장착**

`PlaygroundPage` 안 `const previewRef = useRef<HTMLElement>(null);` 줄 바로 아래에 추가:
```tsx
  const [createOpen, setCreateOpen] = useState(false);
```

그리고 기존 `<>` fragment 의 `</>` 바로 안쪽(맨 아래)에 추가:
```tsx
      <CreateProjectFab onClick={() => setCreateOpen(true)} />
      <CreateProjectDialog
        open={createOpen}
        onClose={() => setCreateOpen(false)}
        light={light}
        dark={dark}
        radius={radius}
        mode={mode}
      />
```

- [ ] **Step 3: 타입 체크 + dev 서버 실행**

```bash
cd /Users/gimsanghyeon/development/PROJECT/sh-ui/apps/docs && pnpm typecheck
cd /Users/gimsanghyeon/development/PROJECT/sh-ui && pnpm dev
```

브라우저에서 `http://localhost:3000/playground` 열기.

- [ ] **Step 4: 수동 브라우저 검증**

다음을 직접 확인:
- [ ] 우하단에 `＋ 프로젝트 만들기` FAB 가 보인다
- [ ] 클릭 시 다이얼로그가 열린다
- [ ] 테마 요약 배지의 색 4 개가 현재 모드 기준으로 그려진다
- [ ] 플레이그라운드에서 배경색을 바꾸면 다이얼로그 재열기 시 배지·명령어가 갱신된다
- [ ] Platform 토글 `Flutter` 선택 시 구조·플러그인 섹션이 사라진다
- [ ] 패키지 매니저 탭 전환 시 명령어 프리픽스가 `pnpm dlx` → `npx` 등으로 바뀐다
- [ ] `명령어 복사` 버튼이 복사 후 1.8 초간 `복사됨` 으로 바뀐다
- [ ] Esc 로 다이얼로그가 닫힌다
- [ ] 다이얼로그 내부 탭 키 포커스 이동이 순환한다

이상이 있으면 각 파일 수정 후 해당 단계부터 재검증.

- [ ] **Step 5: 복사한 명령어로 실제 프로젝트 생성 검증 (전체 워크플로)**

1. 플레이그라운드에서 `--primary` 를 빨강(#DC2626)으로 바꾸고 `--radius` 를 `1.0rem` 으로 설정
2. 다이얼로그 열어 `프로젝트 이름: pg-test`, `Platform: next`, `Structure: standalone`, `Plugins: 없음`, `pm: pnpm` 선택
3. 명령어 복사
4. `cd /tmp && rm -rf pg-test` 후 복사한 명령어 실행

단, 공식 레지스트리의 `@sh-ui/create@0.18.0` 가 이미 퍼블리시됐어야 한다 (Task 13 완료). 로컬 CLI 로 검증하려면 명령어의 `pnpm dlx @sh-ui/create` 부분을 `node /Users/gimsanghyeon/development/PROJECT/sh-ui/packages/create/bin/create.js` 로 대체.

기대:
```bash
grep '^  --primary:' /tmp/pg-test/src/shared/styles/tokens.css | head -1
# → --primary: #DC2626;
grep '^  --radius:' /tmp/pg-test/src/shared/styles/tokens.css
# → --radius: 1rem;
```

- [ ] **Step 6: 임시 프로젝트 정리**

```bash
rm -rf /tmp/pg-test
```

- [ ] **Step 7: 커밋 + push**

```bash
cd /Users/gimsanghyeon/development/PROJECT/sh-ui
git add apps/docs/app/playground/page.tsx
git commit -m "feat(docs): 플레이그라운드에 create-project FAB + Dialog 장착"
git push origin dev
```

---

## Self-Review

### 1. Spec coverage

| 스펙 요구사항 | 구현 태스크 |
|---|---|
| 전체 아키텍처 (docs + CLI 2-layer) | Phase 1 / 2 / 3 전체 |
| FAB + Dialog UI | Task 16, 17, 18 |
| Dialog 로컬 상태 & props | Task 16 |
| 조건부 UI (flutter 시 숨김) | Task 16 |
| 테마 요약 배지 | Task 16 |
| 명령어 프리뷰 + 복사 + aria-live | Task 16 |
| 패키지 매니저 탭 | Task 15 (prefix 맵), 16 (UI) |
| CLI 플래그 (`--platform` 등) | Task 8, 9, 11 |
| `--theme` base64 디코드 + 검증 | Task 5 |
| 템플릿 마커 & 주입 | Task 1, 2, 3, 6, 7, 10 |
| Dart inverse/subtle 보존 | Task 7 |
| 부분 플래그 → 나머지 프롬프트 | Task 9 (scenario 7) |
| `--yes` 플래그 | Task 9 (scenario 6), 11 |
| 에러 케이스 (잘못된 base64, 스키마 오류) | Task 5, 10 (scenario 11), 12 (Step 3) |
| E2E 테스트 | Task 10 (scenario 8, 9, 10, 11), 12, 18 Step 5 |
| 템플릿 마커 검증 | Task 4 |
| 출시 순서 (PR 1 → 2 → 3) | Phase 구분 |
| versions.json 엔트리 | Task 13 |
| MINOR 범프 | Task 13 |

모든 스펙 요구사항에 대응 태스크 존재.

### 2. Placeholder scan
- "TBD", "TODO", "implement later" 등 없음
- 모든 code step 에 실제 코드 포함
- "fill in details" 류 모호한 지시 없음

### 3. Type consistency
- `TOKEN_KEYS` 는 decode.js 에서 export 되어 inject.js / smoke.test 에서 동일하게 import (Task 5, 6, 10)
- `ThemeConfig` 타입 / `TokenKey` 타입은 docs 쪽 encodeTheme.ts 에서 정의, CreateProjectDialog.tsx 에서 동일 이름으로 import (Task 14, 16)
- `ComposerOptions` 필드는 useCommandComposer.ts 와 CreateProjectDialog.tsx 에서 일치 (Task 15, 16)
- `replaceSection(content, section, commentOpen, commentClose, replacement)` 시그니처는 테스트(Task 6 Step 1)와 구현(Step 3)과 호출부(Task 10 Step 3)에서 동일

일관성 문제 없음.

---

## 실행 옵션

**1. Subagent-Driven (권장)** — 태스크마다 새 서브에이전트를 dispatch, 단계 사이 리뷰, 빠른 반복

**2. Inline Execution** — 이 세션에서 직접 실행, 체크포인트로 배치

어떤 방식으로 갈까요?
